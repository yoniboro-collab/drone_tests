[pytest]
# Register the timeout mark so pytest knows it's valid
markers =
    timeout: mark a test with a timeout in seconds

# Silence the DroneKit internal DeprecationWarning (not fixable on our end)
filterwarnings =
    ignore::DeprecationWarning:dronekit